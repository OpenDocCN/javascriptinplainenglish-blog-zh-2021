# 我将在 2021 年使用的 JavaScript 技巧

> 原文：<https://javascript.plainenglish.io/javascript-tricks-i-will-be-using-in-2021-3c59faa5412b?source=collection_archive---------9----------------------->

## *你听说过所有这些吗？*

![](img/824a9b938bca9b124942f70ea6a2702f.png)

Photo by [Artem Sapegin](https://unsplash.com/@sapegin?utm_source=unsplash&utm_medium=referral&utm_content=creditCopyText) on [Unsplash](https://unsplash.com/s/photos/javascript?utm_source=unsplash&utm_medium=referral&utm_content=creditCopyText)

# 介绍

每天，一个新的完成简单任务的方法被编程。有些是你正在使用的语言，有些是框架。有些你可能知道，有些你可能不知道。在我做一些研究之前，我当然不知道所有这些。

所以让我们不要再等了，进入这个列表吧。

# 传播算子

spread 运算符对于将数组复制到可能包含新数据的新数组中非常有用。

这些在 React 中经常被用来处理状态变化，这就是我如何知道这个的。在反应过程中，用于处理状态。

# 将字符串转换为数组

这不是 spread 操作符的唯一技巧。它也可以用来将字符串转换成数组。

不幸的是，这不适用于数字，但它仍然是一个非常有用的特性，可以稍微清理一下您的代码。

# 箭头功能

这些并不完全是新的，但是很多(新的)程序员仍然不知道这个。

乍一看，这似乎很奇怪，特别是因为我们在箭头函数的例子中把花括号放在了一边。如果你想要一个多行的匿名函数，你应该在箭头后面使用花括号。

这些匿名函数在像`.map()`这样需要函数作为参数的函数中非常有用。

# 那个？操作员

快速内联空值检查比无限的 if 和的可读性更好？算我一个。

`?`操作符对于链接空支票很有用，你可以在这里看到。您能想出这种产品的任何使用案例吗？

# 模板文字

模板文字是药物上的字符串。你可以很容易地在它们之间添加变量。您可以使用反斜杠字符```来定义这些。

我有没有提到它也可以多线工作？它将像您预期的那样注册换行符。这对于 GraphQL 查询、SQL 查询以及使用 jQuery 什么的添加元素非常有用。今年我肯定会经常用这个。

# 三元运算符

三元运算符是一种简化的 if 语句，您可以在定义变量时使用它。您使用`?`和`:`来定义这些。

正如你所看到的，你可以将 5 行压缩成一行，这非常简洁。单个的可读性也很好。

如果你愿意，你也可以把它们锁起来，但是不推荐这样做，因为这样很容易失去控制。

这些在 React 中也非常有用，可以根据语句将视图设置为 null 或组件。

# 轻松交换变量

这是给所有学生的。记住有三个变量的练习，你需要用第三个变量交换两个变量。嗯，不会再有了！

现在你可以只使用两个变量来交换变量，这可能有一些有用的用例，但是我现在想不出任何正确的方法。

# 从数组中获取所有唯一性

获得所有的唯一性通常需要一些魔法，但是 JavaScript 的新功能是`Set`。这让事情简单多了。

集合是一种不允许任何重复的新数据类型。我以前用过这个。我相信您也会知道一些用例。

# 字符串到数字

这个对处理文本输入非常有用。

`+`操作员可以快速、干净地将数字串转换成实际数字。

您已经知道所有这些了吗？你还有什么想让别人知道的吗？

不管怎样，祝你今天愉快！