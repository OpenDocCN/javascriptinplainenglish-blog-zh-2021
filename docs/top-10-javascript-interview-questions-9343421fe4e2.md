# 十大 JavaScript 面试问题

> 原文：<https://javascript.plainenglish.io/top-10-javascript-interview-questions-9343421fe4e2?source=collection_archive---------3----------------------->

![](img/f6331e5ada232171c6bb0e30bf7c18fc.png)

If you want to be a JavaScript programmer, you must know these things.

## 1.什么是真值和假值？

**答:**真值如 ***真*** 假值如 ***假*** 。这些措施如下:

```
**falsy values** -> 0, “”, undefined, null, NaN, false
**truthy values**-> “0” , “ “, [], {}, ‘false’
```

因此，如果我们在 if-else 语句中使用这些值中的一个，那么我们将得到相同的输出，如下所示。在这个给定的示例中，我将在 console.log()方法中使用三元表达式向您展示这些值。

示例:

falsy-truthy values

## 2.null 和 undefined 有什么区别？

**答:**首先， **null** 表示空的或者不存在的值。应该是不存在的东西。它可以作为空值的表示形式赋给变量。

另一方面， **undefined** 意味着一个变量已经被声明，但是它的值还没有被赋值。在函数中，如果我们不传递参数，就会显示 undefined。

示例:

## 3.两倍等于(==)和三倍等于(===)有什么区别？

**回答:**双等号`==`是一个抽象的等号运算符。它通过将表达式从一种数据类型更改为另一种数据类型来检查其两个操作数是否相同。

另一方面，Triple equal `===`是一个严格的等式运算符，对于不同类型的值，它返回 false。三重等于不执行类型强制。

示例:

## 4.什么是‘T2’关键词？

**答:**它指的是正在执行 JavaScript 代码当前位的对象。当执行每个 JavaScript 函数时，都有一个对其当前执行上下文的引用，它被称为`this`。

示例:

## 5.如何检查一个质数？

**答案:**质数是只能被 1 和它本身整除的自然数。例如 2，3，5，7 是质数。另一方面，4 不是质数，因为它可以被 1、2 和 4 整除。

示例:

## 6.寻找数组中最大的元素？

**答:**我们可以用 for-loop 方法找到一个数组的最大元素。

示例:

## 7.JavaScript 回调函数怎么用？

**回答:**在 JavaScript 中，我们可以将一个函数作为参数传递给一个函数。在另一个函数内部作为参数传递的这个函数称为回调函数。

在这个给定的代码中，有两个函数，在调用`greet()`函数时，传递了两个参数(一个字符串值和一个函数)。

示例:

## **8。Let 和 Const 有什么区别？**

**回答:**在 Es6 版本的 JavaScript 中，变量可以多两种方式声明。首先，我们可以用关键字 **let** 声明一个变量。

通过应用关键字 **let** ，我们可以不止一次地改变变量的值。如果我们在一个块范围内使用关键字 **let** ，那么我们将不会在这个块范围之外访问它。我们也可以通过应用关键字 **const** 来声明一个变量。但是它的特点是变量值不能改变多次。

示例:

## 9.箭头功能有什么优点？

**答:**箭头函数是函数声明的快捷方式的一种方法。它基本上会**缩短**我们的函数语法。

示例:

## **10。JavaScript 中的闭包:**

**答:**当一个函数能够记住并访问其词法范围时，即使该函数在其词法范围之外执行。

示例:

*更多内容请看*[***plain English . io***](http://plainenglish.io)